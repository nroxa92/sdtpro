name: sdmt_final
description: "A diagnostic tool for SeaDoo watercraft."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  fl_chart: ^0.68.0
  web_socket_channel: ^2.4.0
  provider: ^6.1.2
  socket_io_client: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

